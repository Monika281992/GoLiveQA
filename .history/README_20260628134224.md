# GoLiveQA

Marketing website for **GoLiveQA** — a boutique QA consultancy. Built with Next.js 14 (App Router), TypeScript, and Tailwind CSS.

## Stack

| | |
|---|---|
| Framework | Next.js 14 (App Router) |
| Language | TypeScript + JSX |
| Styling | Tailwind CSS + inline styles |
| Email | Resend (`RESEND_API_KEY`) |
| Icons | lucide-react |
| Deployment | Netlify |

## Getting started

```bash
npm install
npm run dev        # http://localhost:3000
npm run build      # production build
```

Create a `.env.local` file in the root:

```
RESEND_API_KEY=your_resend_api_key
```

## Pages

| Route | Description |
|---|---|
| `/` | Homepage — Hero, Problems We Solve, How We Work, Pricing CTA, Contact |
| `/manual-testing` | Manual Testing service page |
| `/automation-coverage` | Automation Testing service page (Playwright / Cypress) |
| `/quality-engineering` | QA Consultation service page |
| `/book-a-call` | Multi-step qualification form |

## Project structure

```
app/
  layout.tsx                  # Global fonts, metadata
  page.tsx                    # Homepage
  globals.css                 # Tailwind directives + base styles
  manual-testing/page.tsx
  automation-coverage/page.tsx
  quality-engineering/page.tsx
  book-a-call/page.tsx
  api/
    contact/route.ts          # Contact form submission
    book/route.ts             # Email capture (hero)
    book-call/route.js        # Full qualification form submission

components/
  navbar.tsx                  # Sticky nav with Services mega-menu
  hero.tsx                    # Hero with email capture CTA
  ProblemsWeSolve.jsx         # Before/after ledger section (homepage)
  process.tsx                 # "How We Work" 4-step section
  pricing.tsx                 # Pricing CTA card
  contact.tsx                 # Contact form
  footer.tsx
  WhatQACovers.jsx            # QA lifecycle timeline (manual-testing page)
  AutomationTesting.jsx       # Automation page content
  CTAEmailCapture.tsx         # Reusable email CTA component
  BookCall.jsx                # Multi-step qualification form
  icons.tsx                   # LinkedIn icon
  ui/
    button.tsx
    card.tsx

lib/
  utils.ts                    # cn() helper
```

## Design tokens

Defined in `tailwind.config.ts`:

| Token | Value | Use |
|---|---|---|
| `accent` | `#c0552f` | Terracotta — buttons, highlights, icons |
| `surface` | `#faf7f2` | Page background |
| `surface-soft` | `#f4ede2` | Alternating sections |
| `hairline` | `#ece4d7` | Borders |
| `ink` | `#111827` | Headings |
| `body` | `#4b5563` | Body text |
| `muted` | `#6b7280` | Secondary text |

## Email

Contact form and book-a-call form both send emails via **Resend** to `monika@goliveqa.com`. The `RESEND_API_KEY` environment variable must be set.


## Add your photo

The About section loads `/monika.jpg`. Put your headshot at:

```
public/monika.jpg
```

It's rendered with `object-contain` (the whole image shows, never cropped). For a fill-the-frame look instead, change `object-contain` → `object-cover` in `components/about.tsx`.

## Project structure

```
app/
  layout.tsx          # fonts (DM Sans + Kaushan Script), metadata
  page.tsx            # composes all sections
  globals.css         # Tailwind directives + base resets
components/
  navbar.tsx          # sticky header
  hero.tsx            # name + tagline + CTA
  tools.tsx           # "Tools we work with" strip
  about.tsx           # bio, signature, photo, stat cards
  companies.tsx       # logos / names worked with
  services.tsx        # 6 service cards
  process.tsx         # 4-step timeline
  pricing.tsx         # "Let's Talk About Your Product" CTA card
  contact.tsx         # contact details + form (client component)
  footer.tsx
  icons.tsx           # inline LinkedIn glyph
  ui/
    button.tsx        # shadcn/ui Button
    card.tsx          # shadcn/ui Card
lib/
  utils.ts            # cn() helper
tailwind.config.ts    # design tokens (colors, fonts, shadows)
```

## Design tokens (tailwind.config.ts)

| Token | Value | Use |
|---|---|---|
| `brand` / `brand-dark` | `#145fdd` / `#0d46b8` | Buttons, links |
| `accent` / `accent-soft` | `#c2613f` / `#f6ebe5` | Eyebrows, badges, signature |
| `surface` / `surface-soft` | `#faf7f2` / `#f4ede2` | Page + alternating sections |
| `hairline` | `#ece4d7` | Borders |
| `ink` / `body` / `muted` / `faint` | `#111827` / `#4b5563` / `#6b7280` / `#9ca3af` | Text |

To recolor the whole site, edit those values in `tailwind.config.ts`.

## Notes

- The contact form is non-functional (prevents default submit). Wire it to your email service / API route (e.g. Resend, Formspree, a Next.js route handler).
- shadcn/ui `Button` and `Card` are included directly (no CLI needed). Add more shadcn components with `npx shadcn@latest add <name>` if you extend the site.
- All copy is preserved verbatim from the original site.
